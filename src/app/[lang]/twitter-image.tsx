/**
 * Same bridge card as Open Graph — Twitter/X large preview.
 * Kept as a twin file so twitter:image is explicit in metadata.
 */
export { alt, contentType, default, size } from "./opengraph-image";
